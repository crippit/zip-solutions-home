---
const products = [
  {
    id: 1,
    name: "Zip Captions",
    appUrl: "https://zipcaptions.app",
    learnUrl: "/zipcaptions",
    description: "Zip Captions is a free, easy-to-use live captioning tool that works directly in your web browser. It provides real-time text transcription of audio content, making it accessible to people who are deaf or hard of hearing.",
    categories: ["Web App"],
    icon: "message-square",
    color: "bg-amethyst",
    borderColor: "border-purple-200",
  },
  {
    id: 2,
    name: "Zip EasySpeak",
    appUrl: "https://easyspeak.zipsolutions.org",
    learnUrl: "/easyspeak",
    description: "Intuitive AAC (Augmentative and Alternative Communication) web application. Runs right in the browser, available for iOS, Android and more!",
    categories: ["Web App"],
    icon: "layout",
    color: "bg-teal-brand",
    borderColor: "border-teal-200",
  },
  {
    id: 3,
    name: "Zip EasySpeak AI",
    appUrl: "https://easyspeakai.zipsolutions.org",
    learnUrl: "/easyspeak",
    description: "Next-generation AI Powered AAC communication powered by advanced Artificial Intelligence. Runs right from your browser, available for iOS, Android and more!",
    categories: ["AI Tool", "Web App"],
    icon: "brain",
    color: "bg-amethyst",
    borderColor: "border-purple-200",
  },
  {
    id: 4,
    name: "Zip EasySpeak for Education",
    appUrl: "https://easyspeakforedu.zipsolutions.org/",
    learnUrl: "/easyspeak",
    description: "Manage EasySpeak AI from a centralized dashboard designed for educational settings, allowing educators to easily oversee and support their students' communication needs.",
    categories: ["Education", "Admin Dashboard"],
    icon: "graduation-cap",
    color: "bg-emerald-600",
    borderColor: "border-emerald-200",
  },
  {
    id: 5,
    name: "Zip Connect",
    appUrl: "https://www.zipsolutions.org/zipconnect",
    learnUrl: "/zipconnect",
    description: "Seamlessly connect and integrate your communication tools into one unified space.",
    categories: ["Integration"],
    icon: "link",
    color: "bg-indigo-600",
    borderColor: "border-indigo-200",
  },
  {
    id: 6,
    name: "ZipPanel",
    appUrl: "https://www.zipsolutions.org/zippanel",
    learnUrl: "/zippanel",
    description: "Show your Zip Captions in a persistent overlay in Google Chrome.",
    categories: ["Extension", "Integration"],
    icon: "layers",
    color: "bg-amber-600",
    borderColor: "border-amber-200",
  },
  {
    id: 7,
    name: "Zip Captions for VMix",
    appUrl: "https://www.zipsolutions.org/zipvmix",
    learnUrl: "/zipvmix",
    description: "Professional Zip Captions live captioning integration directly into your VMix broadcasting workflow.",
    categories: ["Integration"],
    icon: "video",
    color: "bg-indigo-600",
    borderColor: "border-indigo-200",
  },
  {
    id: 8,
    name: "Zip Speech",
    appUrl: "https://chromewebstore.google.com/detail/zip-speech/abmneamgapjekkemjihkhokdgpdeejff",
    learnUrl: "/zipspeech",
    description: "A browser extension which brings AAC accessibility to Google Meet.",
    categories: ["Extension"],
    icon: "mic",
    color: "bg-amber-600",
    borderColor: "border-amber-200",
  },
  {
    id: 9,
    name: "Zip Captions Companion",
    appUrl: "https://chromewebstore.google.com/detail/zip-captions-companion-ad/chcflkiceakimokommplmfpcpodabdcg",
    learnUrl: "/zipcaptionscompanion",
    description: "Companion Chrome extension designed to enhance and expand your Zip Captions experience.",
    categories: ["Extension"],
    icon: "grid",
    color: "bg-amber-600",
    borderColor: "border-amber-200",
  },
  {
    id: 10,
    name: "EquiAssess",
    appUrl: "https://equiassess.zipsolutions.org",
    learnUrl: null,
    description: "A secure platform for educational establishments to administer accessible summative assessments. Designed to be easy to set up and use, EquiAssess provides a range of accessibility features to ensure that all students can demonstrate their knowledge and skills on an equal footing.",
    categories: ["Education"],
    icon: "shield-check",
    color: "bg-emerald-600",
    borderColor: "border-emerald-200",
  },
  {
    id: 11,
    name: "EquiBrowse",
    appUrl: "https://chromewebstore.google.com/detail/equibrowse-by-zip-solutio/alahhcjbblegneebpjehobaiemckblhl?authuser=0&hl=en-GB",
    learnUrl: "/equibrowse",
    description: "Accessibility built right into your browser. Read text aloud, highlight as you read, get definitions and access the picture dictionary!",
    categories: ["Education", "Extension"],
    icon: "book-open",
    color: "bg-emerald-600",
    borderColor: "border-emerald-200",
  },
  {
    id: 12,
    name: "Assistive Listening System Consultancy",
    appUrl: null,
    learnUrl: "/contact-us",
    description: "Assistive Listening provides those with hearing loss the ability to have better access to sound. Get in touch to learn about our innovative install your own system model, where we send you detailed instructions and a shopping list.",
    categories: ["Consulting"],
    icon: "headphones",
    color: "bg-teal-brand",
    borderColor: "border-teal-200",
  },
  {
    id: 13,
    name: "Digital Sound & Lighting Systems",
    appUrl: null,
    learnUrl: "/contact-us",
    description: "Get in touch to learn about how we can help to integrate systems in professional theatre spaces, as well as AV systems in conference and educational settings. We also have experience with architectural lighting systems such as ETC Mosaic as well as the integration and automation of these systems with others.",
    categories: ["Consulting"],
    icon: "sliders",
    color: "bg-teal-brand",
    borderColor: "border-teal-200",
  },
  {
    id: 14,
    name: "Free Resources for Listening Accessibility",
    appUrl: "https://resources.zipsolutions.org",
    learnUrl: "https://resources.zipsolutions.org",
    description: "We have a whole set of resources dedicated to getting started with listening accessibility, including: Setting up SMART Technologies products with Assistive Listening products such as Phonak Roger, Setting up Interactive Flat Panel & TVs with Assistive Listening products, Understanding the Roger Inspiro, Using the Roger Touchscreen & Roger Media Hub in the classroom, WiFi based assistive listening systems, Getting started with Surtitles for Theatre captioning, and many more.",
    categories: ["Consulting"],
    icon: "library",
    color: "bg-teal-brand",
    borderColor: "border-teal-200",
  },
  {
    id: 15,
    name: "Captioning Consultancy",
    appUrl: null,
    learnUrl: "/contact-us",
    description: "As the creators of ZipCaptions we have considerable experience with Live Captions. We can help you to integrate live captions at your next event. We also have experience with the implementation of Surtitles, the captioning used in live theatre based on a script. We can help you to set up surtitles for your event, as well as with how to best display the surtitles on your stage.",
    categories: ["Consulting"],
    icon: "users",
    color: "bg-teal-brand",
    borderColor: "border-teal-200",
  },
  {
    id: 16,
    name: "In-Person & Hybrid Event Organisation",
    appUrl: null,
    learnUrl: "/contact-us",
    description: "Turnkey in-person, hybrid, and virtual event organisation for educational institutions and organizations, complete with multi-room streaming, remote presenter integration, and accompanying Custom Professional Development (PD) workshops.",
    categories: ["Education", "Consulting", "Events & PD"],
    icon: "calendar",
    color: "bg-amethyst",
    borderColor: "border-purple-200",
  },
  {
    id: 17,
    name: "Custom Zip Captions Live Event Solutions",
    appUrl: null,
    learnUrl: "/contact-us",
    description: "Customized live event captioning solutions for auditorium keynotes, theater surtitles, multi-track conferences, and live broadcasts. We provide customized quotes, tailored display setups, and dedicated SLA engineering.",
    categories: ["Consulting", "Integration", "Events & PD"],
    icon: "video",
    color: "bg-teal-brand",
    borderColor: "border-teal-200",
  }
];

const allCategories = ["All", "Events & PD", "Web App", "AI Tool", "Education", "Integration", "Extension", "Consulting", "Admin Dashboard"];
---

<div class="w-full">
  
  <!-- Search & Filter Controls -->
  <div class="mb-10 space-y-4">
    <div class="flex flex-col md:flex-row gap-4 items-center justify-between">
      
      <!-- Search Input -->
      <div class="relative w-full md:w-96">
        <div class="absolute inset-y-0 left-0 pl-3.5 flex items-center pointer-events-none text-slate-400">
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </div>
        <input 
          id="product-search"
          type="text" 
          placeholder="Search products, services, tools..." 
          class="w-full pl-10 pr-4 py-2.5 rounded-2xl border border-slate-200 bg-white text-midnight placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-amethyst shadow-sm transition-all text-sm"
          aria-label="Search products and services"
        />
      </div>

      <!-- Result Counter -->
      <div id="product-count" class="text-xs font-semibold text-slate-500 uppercase tracking-wider">
        Showing all {products.length} solutions
      </div>
    </div>

    <!-- Category Filters -->
    <div class="flex flex-wrap gap-2 pt-2" role="tablist" aria-label="Filter by category">
      {allCategories.map((cat) => (
        <button
          type="button"
          data-category={cat}
          class={`filter-btn px-3.5 py-1.5 rounded-xl text-xs font-semibold transition-all border ${
            cat === 'All'
              ? 'bg-midnight text-white border-midnight active-filter'
              : 'bg-white text-slate-600 border-slate-200 hover:border-amethyst/50 hover:text-amethyst'
          }`}
          role="tab"
          aria-selected={cat === 'All' ? 'true' : 'false'}
        >
          {cat}
        </button>
      ))}
    </div>
  </div>

  <!-- Product Grid -->
  <div id="products-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {products.map((product) => (
      <div 
        class="product-card group flex flex-col bg-white rounded-3xl p-6 border border-slate-200/80 shadow-sm hover:shadow-xl hover:-translate-y-1 transition-all duration-300 relative overflow-hidden"
        data-name={product.name.toLowerCase()}
        data-desc={product.description.toLowerCase()}
        data-categories={JSON.stringify(product.categories)}
      >
        <!-- Background Ambient Accent Blob -->
        <div class={`absolute -right-10 -top-10 w-28 h-28 rounded-full opacity-10 group-hover:opacity-25 transition-opacity duration-300 ${product.color}`}></div>

        <!-- Top row: Icon and Category text (No pills) -->
        <div class="flex items-start justify-between mb-5 relative z-10">
          <div class={`p-3.5 rounded-2xl text-white shadow-md ${product.color}`}>
            <!-- Icon SVG fallback -->
            <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
            </svg>
          </div>
          <div class="flex justify-end max-w-[65%] text-right">
            <span class="text-xs font-semibold text-slate-500 tracking-wide uppercase">
              {product.categories.join(' · ')}
            </span>
          </div>
        </div>

        <!-- Title -->
        <h2 class="text-xl font-bold font-heading text-midnight mb-2.5 group-hover:text-amethyst transition-colors">
          {product.name}
        </h2>

        <!-- Description -->
        <p class="text-slate-600 text-sm leading-relaxed mb-6 flex-grow">
          {product.description}
        </p>

        <!-- Actions -->
        <div class="mt-auto flex items-center gap-2.5 pt-2 border-t border-slate-100">
          {product.learnUrl && (
            <a 
              href={product.learnUrl}
              class="flex-1 flex justify-center items-center px-3.5 py-2.5 bg-slate-50 hover:bg-slate-100 text-slate-800 text-xs font-bold rounded-xl border border-slate-200 transition-colors"
            >
              Learn More
            </a>
          )}
          {product.appUrl && (
            <a 
              href={product.appUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="flex-1 flex justify-center items-center gap-1.5 px-3.5 py-2.5 bg-amethyst hover:bg-amethyst-dark text-white text-xs font-bold rounded-xl shadow-sm hover:shadow-md transition-all"
            >
              <span>Try Now</span>
              <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
              </svg>
            </a>
          )}
        </div>

      </div>
    ))}
  </div>

  <!-- Empty state message -->
  <div id="no-products" class="hidden text-center py-16 bg-white rounded-3xl border border-slate-200/80 p-8">
    <svg class="w-12 h-12 mx-auto text-slate-300 mb-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>
    <h3 class="text-lg font-bold text-midnight mb-1 font-heading">No solutions match your search</h3>
    <p class="text-sm text-slate-500 mb-4">Try adjusting your keywords or clearing the category filter.</p>
    <button id="reset-filters-btn" class="px-4 py-2 bg-slate-100 hover:bg-slate-200 text-slate-700 text-xs font-bold rounded-xl transition-colors">
      Reset Filters
    </button>
  </div>

</div>

<script>
  let activeCategory = 'All';
  let searchTerm = '';

  const searchInput = document.getElementById('product-search') as HTMLInputElement;
  const filterBtns = document.querySelectorAll('.filter-btn');
  const productCards = document.querySelectorAll('.product-card');
  const countLabel = document.getElementById('product-count');
  const noProducts = document.getElementById('no-products');
  const resetBtn = document.getElementById('reset-filters-btn');

  function updateFilter() {
    let visibleCount = 0;

    productCards.forEach((card) => {
      const el = card as HTMLElement;
      const name = el.dataset.name || '';
      const desc = el.dataset.desc || '';
      const categories: string[] = JSON.parse(el.dataset.categories || '[]');

      const matchesSearch = !searchTerm || name.includes(searchTerm) || desc.includes(searchTerm);
      const matchesCategory = activeCategory === 'All' || categories.includes(activeCategory);

      if (matchesSearch && matchesCategory) {
        el.style.display = 'flex';
        visibleCount++;
      } else {
        el.style.display = 'none';
      }
    });

    if (countLabel) {
      countLabel.textContent = `Showing ${visibleCount} solutions`;
    }

    if (noProducts) {
      if (visibleCount === 0) {
        noProducts.classList.remove('hidden');
      } else {
        noProducts.classList.add('hidden');
      }
    }
  }

  searchInput?.addEventListener('input', (e) => {
    searchTerm = (e.target as HTMLInputElement).value.trim().toLowerCase();
    updateFilter();
  });

  filterBtns.forEach((btn) => {
    btn.addEventListener('click', () => {
      filterBtns.forEach((b) => {
        b.classList.remove('bg-midnight', 'text-white', 'border-midnight', 'active-filter');
        b.classList.add('bg-white', 'text-slate-600', 'border-slate-200');
        b.setAttribute('aria-selected', 'false');
      });

      btn.classList.add('bg-midnight', 'text-white', 'border-midnight', 'active-filter');
      btn.classList.remove('bg-white', 'text-slate-600', 'border-slate-200');
      btn.setAttribute('aria-selected', 'true');

      activeCategory = btn.getAttribute('data-category') || 'All';
      updateFilter();
    });
  });

  resetBtn?.addEventListener('click', () => {
    if (searchInput) searchInput.value = '';
    searchTerm = '';
    activeCategory = 'All';

    filterBtns.forEach((b) => {
      if (b.getAttribute('data-category') === 'All') {
        b.classList.add('bg-midnight', 'text-white', 'border-midnight', 'active-filter');
        b.classList.remove('bg-white', 'text-slate-600', 'border-slate-200');
        b.setAttribute('aria-selected', 'true');
      } else {
        b.classList.remove('bg-midnight', 'text-white', 'border-midnight', 'active-filter');
        b.classList.add('bg-white', 'text-slate-600', 'border-slate-200');
        b.setAttribute('aria-selected', 'false');
      }
    });

    updateFilter();
  });
</script>
